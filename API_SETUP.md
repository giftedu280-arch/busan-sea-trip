# API 연결 안내

## 1. 날씨 API

이 앱은 **Open-Meteo Forecast API**를 사용합니다. 비상업적·교육용 이용에는 회원가입이나 API 키 발급이 필요하지 않습니다.

- 현재 위치에서 선택한 부산 구의 대표 좌표를 사용합니다.
- 여행 날짜를 기준으로 날씨, 최고·최저 기온, 강수확률을 가져옵니다.
- 일반 예보는 오늘부터 최대 16일까지 확인할 수 있습니다.
- 인터넷 연결이 필요합니다.

## 2. 카카오 지도 API

추천 경로를 웹페이지 안의 카카오맵에 표시하려면 카카오디벨로퍼스의 JavaScript 키가 필요합니다.

1. `https://developers.kakao.com`에서 카카오 계정으로 로그인합니다.
2. **내 애플리케이션 → 애플리케이션 추가하기**를 선택합니다.
3. 만든 앱의 **앱 설정 → 플랫폼 키**에서 JavaScript 키를 확인합니다.
4. **카카오맵 → 사용 설정**을 켭니다.
5. JavaScript 키의 웹 도메인에 실제 사이트 주소를 등록합니다. 로컬 서버라면 `http://localhost:8000`을 등록합니다.
6. JavaScript 키를 복사합니다. REST API 키가 아닙니다.
7. 앱 결과 화면에서 **지도 API 설정**을 누르고 JavaScript 키를 입력합니다.

`file://`로 직접 연 페이지는 카카오맵 도메인 인증이 제한될 수 있습니다. 폴더에서 간단한 로컬 서버를 실행한 뒤 `http://localhost:8000`으로 접속하거나 GitHub Pages 주소를 웹 도메인에 등록하세요.

이 프로젝트에서는 `로컬서버로_실행하기.bat`를 더블클릭하면 `http://localhost:8000` 주소로 자동 실행됩니다. 검은 실행 창은 웹페이지를 사용하는 동안 닫지 마세요.

REST API 키나 Admin 키는 입력하지 마세요. 이 앱의 지도 표시에는 JavaScript 키만 사용합니다.
